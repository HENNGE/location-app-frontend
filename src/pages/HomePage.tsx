import { Autocomplete } from '@mantine/core';

const HomePage = (): JSX.Element => {
    return (
        <div>
            <Autocomplete placeholder='Seach for a user...' />
        </div>
    );
};

export default HomePage;
